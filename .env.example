# Environment Variables for Canopy Monorepo
#
# This file contains shared environment variables used across multiple packages.
# Copy this file to `.env` in the root directory to get started.
#
# Turborepo Configuration:
# - Variables listed in turbo.json's `globalEnv` are available to all packages
# - Changes to these variables will invalidate Turborepo's cache
# - Framework-specific prefixes (NEXT_PUBLIC_*, VITE_*) are auto-inferred
#
# For package-specific variables, create a .env file in that package's directory.

# Database (Neon PostgreSQL)
# Get your connection string from: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=

# Example:
# DATABASE_URL=postgresql://username:password@ep-cool-darkness-123456.us-east-2.aws.neon.tech/canopy?sslmode=require
